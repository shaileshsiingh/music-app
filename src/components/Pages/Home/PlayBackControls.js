// src/home/PlaybackControls.js
import React from 'react';

const PlaybackControls = () => {
  return (
    <div>
      {/* Playback control buttons */}
      <button>Play</button>
      <button>Pause</button>
      <button>Skip</button>
    </div>
  );
};

export default PlaybackControls;
